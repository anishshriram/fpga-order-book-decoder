# build / sim artifacts
build/
*.vcd
*.json
*.fs
*.o

# generated data feeds
data/*.hex
data/*.bin
data/*.txt
data/*.vh

# toolchain (installed at ~/oss-cad-suite, never in-tree)
oss-cad-suite/
*.tgz

# raw ITCH capture files (huge, not redistributable)
*.NASDAQ_ITCH50
*_ITCH50
*.NASDAQ_ITCH50.gz

# python
__pycache__/
*.pyc

# macOS
.DS_Store
